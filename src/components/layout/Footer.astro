---
import SocialLinks from '@components/ui/SocialLinks.astro';
import type { Locale, SiteContent } from '@i18n/types';
import {
  SITE_NAME,
  CONTACT_EMAIL,
  CONTACT_PHONE,
  ADDRESS,
  SITE_URL,
} from '@data/site';

interface Props {
  locale: Locale;
  content: SiteContent;
}

const { content } = Astro.props;
const { ui, nav } = content;
---

<footer role="contentinfo" class="bg-brand-navy text-white">
  <div class="mx-auto max-w-7xl px-4 py-16 sm:px-6 lg:px-8">
    <div class="grid grid-cols-1 gap-12 md:grid-cols-2 lg:grid-cols-4">
      <div class="lg:col-span-2">
        <div class="flex flex-wrap items-center gap-3 sm:gap-4">
          <img
            src="/images/SICSA%20GROUP%20FINAL_BLANCO.png"
            alt="SICSA GROUP"
            width="2782"
            height="1083"
            class="h-[2.475rem] w-auto shrink-0"
            loading="lazy"
            decoding="async"
          />
          <span class="h-8 w-px bg-white/50 sm:h-10" aria-hidden="true"></span>
          <img
            src="/images/Logo%20Santal%20Group_BLANCO_1.png"
            alt="SANTAL GROUP"
            width="3000"
            height="945"
            class="h-[2.475rem] w-auto shrink-0"
            loading="lazy"
            decoding="async"
          />
        </div>
        <p class="mt-4 max-w-md text-sm leading-relaxed text-white/70">
          {ui.footerTagline}
        </p>
        <div class="mt-6">
          <SocialLinks light followPrefix={ui.socialFollowPrefix} />
        </div>
      </div>

      <div>
        <h3 class="font-display text-sm font-semibold uppercase tracking-wider text-white">
          {ui.footerNavigation}
        </h3>
        <ul class="mt-4 space-y-2">
          {
            nav.map((link) => (
              <li>
                <a
                  href={`#${link.hash}`}
                  class="text-sm text-white/70 transition hover:text-white focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-brand-red"
                >
                  {link.label}
                </a>
              </li>
            ))
          }
        </ul>
      </div>

      <div>
        <h3 class="font-display text-sm font-semibold uppercase tracking-wider text-white">
          {ui.footerContact}
        </h3>
        <ul class="mt-4 space-y-3 text-sm text-white/70">
          <li>
            <a href={`tel:${CONTACT_PHONE.replace(/\s/g, '')}`} class="hover:text-white">
              PBX: {CONTACT_PHONE}
            </a>
          </li>
          <li>
            <a href={`mailto:${CONTACT_EMAIL}`} class="hover:text-white">
              {CONTACT_EMAIL}
            </a>
          </li>
          <li>
            <address class="not-italic leading-relaxed">
              {ADDRESS.street}<br />
              {ADDRESS.building}<br />
              {ADDRESS.city}, {ADDRESS.country}
            </address>
          </li>
        </ul>
      </div>
    </div>

    <div
      class="mt-12 flex flex-col items-center justify-between gap-4 border-t border-white/10 pt-8 sm:flex-row"
    >
      <p class="text-sm text-white/50">
        &copy; {new Date().getFullYear()} {SITE_NAME}. {ui.footerCopyright}
      </p>
      <a href={SITE_URL} class="text-sm text-white/50 hover:text-white">
        {SITE_URL.replace('https://', '')}
      </a>
    </div>
  </div>
</footer>
