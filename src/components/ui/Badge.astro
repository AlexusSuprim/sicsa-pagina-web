---
interface Props {
  text: string;
}

const { text } = Astro.props;
---

<span
  class="inline-flex items-center rounded-full bg-brand-red/10 px-3 py-1 text-sm font-semibold uppercase tracking-wider text-brand-red"
>
  {text}
</span>
